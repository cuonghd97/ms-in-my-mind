server:
  port: 8110

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ms_in_my_mind?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
    show-sql: true

userService:
  app:
    jwtCookieName: user-service
    jwtSecret: ===========================Spring===========================
    jwtExpirationMS: 86400000

