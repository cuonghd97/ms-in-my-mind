server:
  port: 8112

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopping_cart
    username: root
    password: root


  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true

productService:
  app:
    jwtSecret: ===========================Spring===========================
    jwtExpirationMS: 86400000

services:
    product: "http://127.0.0.1:8111/api"